---
phase: 01-learning-path-and-metadata-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - docs/index.md
  - docs/reading-map.md
  - docs/practice/index.md
  - docs/intermediate/index.md
  - .vitepress/theme/components/PracticePreview.vue
  - .vitepress/theme/components/PracticePhaseGrid.vue
  - .vitepress/config.mts
autonomous: true
requirements:
  - IA-02
  - IA-03
  - DISC-02
must_haves:
  truths:
    - "首页、阅读地图、实践页和中级篇入口都使用相同的板块定位与路径入口数据。"
    - "用户在首页和导航中可以直接发现 `/learning-paths/`，并理解理论篇 / 实践篇 / 中级篇的推荐进入方式。"
    - "实践阶段摘要不再以内联数组散落在 markdown 中，而是复用统一数据层。"
  artifacts:
    - path: "docs/index.md"
      provides: "首页路径入口与板块定位"
    - path: "docs/reading-map.md"
      provides: "阅读地图中的 canonical path 入口"
    - path: "docs/practice/index.md"
      provides: "实践篇入口与阶段摘要接入统一数据"
    - path: "docs/intermediate/index.md"
      provides: "中级篇定位与回链入口接入统一数据"
    - path: ".vitepress/config.mts"
      provides: "全站 nav / sidebar 暴露学习路径页"
  key_links:
    - from: "docs/index.md"
      to: "/learning-paths/"
      via: "首页 CTA 与核心学习路径区"
      pattern: "/learning-paths/"
    - from: "docs/practice/index.md"
      to: ".vitepress/theme/components/PracticePhaseGrid.vue"
      via: "移除 inline phase 数组"
      pattern: "<PracticePhaseGrid"
    - from: ".vitepress/config.mts"
      to: "docs/learning-paths/index.md"
      via: "nav / sidebar link"
      pattern: "学习路径"
---

<objective>
把新的元数据和路径体系接入现有核心入口页，让用户在真实站点里看到统一的信息架构，而不是只存在一个独立路径页。

Purpose: 完成 Phase 1 的“统一数据源落地”，确保首页、阅读地图、实践页和中级篇入口不再各说各话。
Output: 数据驱动的首页入口组件、统一的实践阶段摘要、学习路径 nav 暴露和入口页对齐。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-02-PLAN.md
@docs/index.md
@docs/reading-map.md
@docs/practice/index.md
@docs/intermediate/index.md
@.vitepress/config.mts

<interfaces>
Executor should reuse the `01-02` data exports instead of inventing new arrays:

```ts
export const sectionIndex: SectionRoleSummary[]
export const sectionById: Record<'theory' | 'practice' | 'intermediate', SectionRoleSummary>
export const learningPaths: LearningPathDefinition[]
export const practicePhases: PracticePhaseSummary[]
```

Components to keep data-driven:
- `LearningPath.vue`: optional `pathIds` filter, no internal hardcoded phase list
- `PracticePhaseGrid.vue`: optional `phases` prop, but default source must be `practicePhases`
- `PracticePreview.vue`: counts and CTA labels derived from `sectionById` / `practicePhases`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构首页与实践入口组件，改为消费统一路径/板块数据</name>
  <files>
docs/index.md
.vitepress/theme/components/PracticePreview.vue
.vitepress/theme/components/PracticePhaseGrid.vue
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
- docs/index.md
- .vitepress/theme/components/PracticePreview.vue
- .vitepress/theme/components/PracticePhaseGrid.vue
- .vitepress/theme/data/content-index.data.ts
- .vitepress/theme/data/learning-paths.data.ts
  </read_first>
  <action>更新 `docs/index.md`，在 hero 或辅助入口区域增加指向 `/learning-paths/` 的显式入口；把“理论篇 / 实践篇 / 中级篇”静态说明替换为 `SectionRoleGrid` 组件；保留 `<LearningPath />`，但它现在必须显示 `theory-first`、`practice-first`、`engineering-depth` 三条目标导向路径而不是旧的阶段数组。重构 `PracticePreview.vue`，移除 `16 / 23 / 7` 的默认常量，改为从 `sectionById.practice` 与 `practicePhases` 计算实践项目总数、阶段数，并从 `sectionById.theory` / `sectionById.intermediate` 读取对应入口标签与推荐入口，不要再把数组索引当成业务语义。入口文案使用固定三类标签：`先读源码`、`先动手实践`、`带着问题进阶`。重构 `PracticePhaseGrid.vue`，让 `phases` props 变为可选；未传入时默认读取 `practicePhases`，这样首页和实践页都能共享同一来源。</action>
  <verify>
    <automated>bun run build:strict</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "/learning-paths/" docs/index.md` 至少返回 1 条结果。
- `rg -n "16,|23,|7" .vitepress/theme/components/PracticePreview.vue` 无结果。
- `rg -n "sectionById" .vitepress/theme/components/PracticePreview.vue` 至少返回 1 条结果。
- `rg -n "practicePhases" .vitepress/theme/components/PracticePhaseGrid.vue .vitepress/theme/components/PracticePreview.vue` 至少返回 2 条结果。
- `bun run build:strict` 退出码为 0。
  </acceptance_criteria>
  <done>首页和首页入口组件已经只消费统一数据层，不再持有自己的学习路径和计数真源。</done>
</task>

<task type="auto">
  <name>Task 2: 让阅读地图、实践篇、中级篇和全站导航对齐新的学习路径体系</name>
  <files>
docs/reading-map.md
docs/practice/index.md
docs/intermediate/index.md
.vitepress/config.mts
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
- docs/reading-map.md
- docs/practice/index.md
- docs/intermediate/index.md
- .vitepress/config.mts
- .vitepress/theme/data/learning-paths.data.ts
  </read_first>
  <action>更新 `.vitepress/config.mts`，在顶层 `nav` 中新增 `学习路径` -> `/learning-paths/`，并在 `'/'` sidebar 顶部加入同名入口。更新 `docs/reading-map.md`，在“推荐起点”之前加入一段新的 canonical path 入口区，局部导入 `<LearningPath :path-ids="['theory-first','practice-first','engineering-depth']" />`，保留后文详细路线 A-E 作为补充说明。更新 `docs/practice/index.md`，删除当前内联 `:phases="[{ id: 1 ... }]"` 数组，直接渲染 `<PracticePhaseGrid />`；在“实践篇解决什么问题”前加入一段与 `sectionById.practice` 完全一致的定位说明和 `/learning-paths/` 回链。更新 `docs/intermediate/index.md`，在标题下新增一段与 `sectionById.intermediate` 完全一致的定位说明，强调它是 `bridge` 入口，并加入返回 `/learning-paths/` 的 CTA。不要在这些页面里复制新的路径定义数组或 phase 数组。</action>
  <verify>
    <automated>bun run build:strict</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "学习路径" .vitepress/config.mts` 至少返回 2 条结果。
- `rg -n "<LearningPath" docs/reading-map.md` 返回至少 1 条结果。
- `rg -n ":phases=\"\\[" docs/practice/index.md` 无结果。
- `rg -n "/learning-paths/" docs/practice/index.md docs/intermediate/index.md docs/reading-map.md` 至少返回 3 条结果。
- `bun run build:strict` 退出码为 0。
  </acceptance_criteria>
  <done>四个核心入口页与全站导航已经接入统一学习路径和板块定位，用户可以在真实站点里按目标而不是只按目录找到入口。</done>
</task>

</tasks>

<verification>
以 `bun run build:strict` 为最终门槛。额外人工检查应打开 `/`、`/learning-paths/`、`/practice/`、`/intermediate/` 和 `/reading-map`，确认理论篇 / 实践篇 / 中级篇的定位文案和推荐入口一致，且首页与导航都能发现 `/learning-paths/`。
</verification>

<success_criteria>
首页、阅读地图、实践篇和中级篇入口都已接入同一数据层；`学习路径` 成为全站一等入口；实践阶段摘要不再散落在 markdown 内联数组里。
</success_criteria>

<output>
After completion, create `.planning/phases/01-learning-path-and-metadata-foundation/01-03-SUMMARY.md`
</output>
