---
phase: 01-learning-path-and-metadata-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .vitepress/theme/data/content-meta.ts
  - .vitepress/theme/components/types.ts
  - scripts/check-learning-metadata.mjs
  - package.json
  - docs/00-what-is-ai-agent/index.md
  - docs/01-agent-basics/index.md
  - docs/03-tool-system/index.md
  - docs/04-session-management/index.md
  - docs/practice/p01-minimal-agent/index.md
  - docs/practice/p10-react-loop/index.md
  - docs/intermediate/27-planning-mechanism/index.md
autonomous: true
requirements:
  - IA-02
  - IA-03
must_haves:
  truths:
    - "首批理论篇、实践篇和中级篇页面都暴露同一套 frontmatter 学习元数据字段。"
    - "缺失或非法的 metadata 会在构建前被脚本拦截，而不是等到页面渲染时才发现。"
    - "内容类型、进入方式和路径引用字段只在一处 TypeScript 合同里定义。"
  artifacts:
    - path: ".vitepress/theme/data/content-meta.ts"
      provides: "学习元数据字面量联合类型、必填字段清单和规范化辅助函数"
    - path: "scripts/check-learning-metadata.mjs"
      provides: "frontmatter 字段、枚举值和路径引用校验"
    - path: "docs/00-what-is-ai-agent/index.md"
      provides: "理论篇 seed metadata"
    - path: "docs/practice/p01-minimal-agent/index.md"
      provides: "实践篇 seed metadata"
    - path: "docs/intermediate/27-planning-mechanism/index.md"
      provides: "中级篇 seed metadata"
  key_links:
    - from: ".vitepress/theme/data/content-meta.ts"
      to: "scripts/check-learning-metadata.mjs"
      via: "共享字段名与枚举集合"
      pattern: "REQUIRED_LEARNING_FRONTMATTER_FIELDS|CONTENT_TYPES|ENTRY_MODES"
    - from: "docs/**/index.md"
      to: ".vitepress/theme/data/learning-paths.data.ts"
      via: "contentId / recommendedNext / practiceLinks"
      pattern: "contentId:|recommendedNext:|practiceLinks:"
---

<objective>
建立 Phase 1 的元数据合同和 Wave 0 校验基础设施，并为后续路径聚合准备可引用的首批内容节点。

Purpose: 先固定“什么是内容节点、有哪些稳定字段、什么算非法 metadata”，避免后续路径页和首页改造建立在松散文案上。
Output: 共享类型合同、metadata 校验脚本、7 个已接入 frontmatter 的核心页面。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-VALIDATION.md
@package.json
@.vitepress/theme/components/types.ts
@scripts/check-content.mjs

<interfaces>
Executor must create these shared contracts in `.vitepress/theme/data/content-meta.ts`:

```ts
export type ContentType = 'theory' | 'practice' | 'intermediate' | 'support'
export type ContentSeries = 'book' | 'practice' | 'intermediate' | 'support'
export type LearningDifficulty = 'beginner' | 'intermediate' | 'advanced'
export type EntryMode = 'read-first' | 'build-first' | 'bridge'
export type SectionId = 'theory' | 'practice' | 'intermediate'

export interface LearningContentFrontmatter {
  contentType: ContentType
  series: ContentSeries
  contentId: string
  shortTitle: string
  summary: string
  difficulty: LearningDifficulty
  estimatedTime: string
  learningGoals: string[]
  prerequisites: string[]
  recommendedNext: string[]
  practiceLinks: string[]
  searchTags: string[]
  navigationLabel: string
  entryMode: EntryMode
  roleDescription: string
}

export interface SectionRoleSummary {
  sectionId: SectionId
  title: string
  roleDescription: string
  entryMode: EntryMode
  recommendedStart: string
  countLabel: string
}

export interface PracticePhaseSummary {
  phaseId: 'phase-1' | 'phase-2' | 'phase-3' | 'phase-4' | 'phase-5' | 'phase-6' | 'phase-7'
  order: 1 | 2 | 3 | 4 | 5 | 6 | 7
  title: string
  subtitle: string
  summary: string
  projectCount: number
  projectIds: string[]
  recommendedStart: string
  themeTags: string[]
}
```

Later plans will reference the exact `contentId` values introduced here:
- `book-00-agent-intro`
- `book-01-agent-basics`
- `book-03-tool-system`
- `book-04-session-management`
- `practice-p01-minimal-agent`
- `practice-p10-react-loop`
- `intermediate-27-planning-mechanism`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固定学习元数据合同与下游组件类型</name>
  <files>
.vitepress/theme/data/content-meta.ts
.vitepress/theme/components/types.ts
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
- .planning/phases/01-learning-path-and-metadata-foundation/01-VALIDATION.md
- .vitepress/theme/components/types.ts
  </read_first>
  <action>新建 `.vitepress/theme/data/content-meta.ts`，导出 `ContentType`、`ContentSeries`、`LearningDifficulty`、`EntryMode`、`SectionId` 五个字面量联合类型；导出 `LearningContentFrontmatter`、`LearningPathStep`、`LearningPathDefinition`、`SectionRoleSummary`、`PracticePhaseSummary` 五个接口；其中 `SectionRoleSummary` 字段固定为 `sectionId`、`title`、`roleDescription`、`entryMode`、`recommendedStart`、`countLabel`，`PracticePhaseSummary` 字段固定为 `phaseId`、`order`、`title`、`subtitle`、`summary`、`projectCount`、`projectIds`、`recommendedStart`、`themeTags`，不要再留“执行时再决定字段”的空间。导出 `REQUIRED_LEARNING_FRONTMATTER_FIELDS` 常量，字段顺序固定为 `contentType`、`series`、`contentId`、`shortTitle`、`summary`、`difficulty`、`estimatedTime`、`learningGoals`、`prerequisites`、`recommendedNext`、`practiceLinks`、`searchTags`、`navigationLabel`、`entryMode`、`roleDescription`。同步扩展 `.vitepress/theme/components/types.ts`，为后续 `LearningPath.vue`、`SectionRoleGrid.vue`、`PracticePhaseGrid.vue` 提供可直接导入的 props / 数据类型。不要把 UI 颜色、图标名、按钮文案塞进合同文件。</action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "export type ContentType = 'theory' \\| 'practice' \\| 'intermediate' \\| 'support'" .vitepress/theme/data/content-meta.ts` 返回 1 条结果。
- `rg -n "REQUIRED_LEARNING_FRONTMATTER_FIELDS" .vitepress/theme/data/content-meta.ts` 返回 1 条结果。
- `rg -n "interface PracticePhaseSummary|phaseId: 'phase-1' \\| 'phase-2' \\| 'phase-3' \\| 'phase-4' \\| 'phase-5' \\| 'phase-6' \\| 'phase-7'|projectIds: string\\[\\]" .vitepress/theme/data/content-meta.ts` 至少返回 3 条结果。
- `rg -n "LearningPathDefinition|SectionRoleSummary|PracticePhaseSummary" .vitepress/theme/components/types.ts` 至少返回 3 条结果。
  </acceptance_criteria>
  <done>共享元数据合同可被 TypeScript 编译通过，且下游组件类型不需要再重复定义 learning path / section summary 结构。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Wave 0 metadata 校验脚本并接入构建命令</name>
  <files>
scripts/check-learning-metadata.mjs
package.json
.vitepress/theme/data/content-meta.ts
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-VALIDATION.md
- scripts/check-content.mjs
- package.json
- .vitepress/theme/data/content-meta.ts
  </read_first>
  <action>新增 `scripts/check-learning-metadata.mjs`，保持 Node ESM 风格和中文报错格式，与现有 `check-content.mjs` 的 fail-fast 模式一致。脚本必须校验两类目标：1) 7 个 seed 页面必须拥有 `REQUIRED_LEARNING_FRONTMATTER_FIELDS` 全部字段；2) 任何已经声明 `contentType` 的页面也必须遵守相同字段和枚举规则。枚举值只允许 `contentType: theory|practice|intermediate|support`、`series: book|practice|intermediate|support`、`difficulty: beginner|intermediate|advanced`、`entryMode: read-first|build-first|bridge`。`recommendedNext` 与 `practiceLinks` 只能是以 `/` 开头的站内路径数组。向 `package.json` 添加 `check:learning-metadata`，并把 `build:strict` 更新为 `bun run check:content && bun run check:practice && bun run check:learning-metadata && bun run build`。</action>
  <verify>
    <automated>node scripts/check-learning-metadata.mjs && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "\"check:learning-metadata\": \"node scripts/check-learning-metadata.mjs\"" package.json` 返回 1 条结果。
- `rg -n "build:strict.*check:learning-metadata" package.json` 返回 1 条结果。
- `node scripts/check-learning-metadata.mjs` 在 Task 3 完成后退出码为 0。
  </acceptance_criteria>
  <done>Phase 1 的 metadata 基础校验已进入标准构建链路，后续 plan 不需要额外手工检查字段完整性。</done>
</task>

<task type="auto">
  <name>Task 3: 为 7 个核心页面补齐可聚合的 frontmatter seed 数据</name>
  <files>
docs/00-what-is-ai-agent/index.md
docs/01-agent-basics/index.md
docs/03-tool-system/index.md
docs/04-session-management/index.md
docs/practice/p01-minimal-agent/index.md
docs/practice/p10-react-loop/index.md
docs/intermediate/27-planning-mechanism/index.md
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
- docs/00-what-is-ai-agent/index.md
- docs/01-agent-basics/index.md
- docs/03-tool-system/index.md
- docs/04-session-management/index.md
- docs/practice/p01-minimal-agent/index.md
- docs/practice/p10-react-loop/index.md
- docs/intermediate/27-planning-mechanism/index.md
  </read_first>
  <action>仅在 frontmatter 中补齐最小可复用字段，不改正文结构。按 3 个子批次执行，降低 7 个页面迁移时的歧义：第 1 批先完成 4 个理论页（`docs/00-what-is-ai-agent/index.md`、`docs/01-agent-basics/index.md`、`docs/03-tool-system/index.md`、`docs/04-session-management/index.md`），第 2 批完成 2 个实践页（`docs/practice/p01-minimal-agent/index.md`、`docs/practice/p10-react-loop/index.md`），第 3 批完成 1 个中级页（`docs/intermediate/27-planning-mechanism/index.md`）；每完成一批都先运行一次 `node scripts/check-learning-metadata.mjs`，再继续下一批。给 7 个页面写入下列精确 `contentId` 和定位字段：`docs/00-what-is-ai-agent/index.md` 使用 `contentType: theory`、`series: book`、`contentId: book-00-agent-intro`、`entryMode: read-first`；`docs/01-agent-basics/index.md` 使用 `book-01-agent-basics`；`docs/03-tool-system/index.md` 使用 `book-03-tool-system`；`docs/04-session-management/index.md` 使用 `book-04-session-management`；`docs/practice/p01-minimal-agent/index.md` 使用 `contentType: practice`、`series: practice`、`contentId: practice-p01-minimal-agent`、`entryMode: build-first`；`docs/practice/p10-react-loop/index.md` 使用 `practice-p10-react-loop`；`docs/intermediate/27-planning-mechanism/index.md` 使用 `contentType: intermediate`、`series: intermediate`、`contentId: intermediate-27-planning-mechanism`、`entryMode: bridge`。每页都要补齐 `shortTitle`、`summary`、`difficulty`、`estimatedTime`、`learningGoals`、`prerequisites`、`recommendedNext`、`practiceLinks`、`searchTags`、`navigationLabel`、`roleDescription`。`recommendedNext` 和 `practiceLinks` 使用以下站内路径：`book-00-agent-intro -> ['/01-agent-basics/', '/practice/p01-minimal-agent/']`；`book-01-agent-basics -> ['/03-tool-system/', '/practice/p01-minimal-agent/']`；`book-03-tool-system -> ['/04-session-management/', '/practice/p10-react-loop/']`；`book-04-session-management -> ['/practice/p10-react-loop/', '/intermediate/27-planning-mechanism/']`；`practice-p01-minimal-agent -> ['/practice/p10-react-loop/', '/00-what-is-ai-agent/']`；`practice-p10-react-loop -> ['/intermediate/27-planning-mechanism/', '/04-session-management/']`；`intermediate-27-planning-mechanism -> ['/intermediate/', '/practice/p10-react-loop/']`。已有页面中的“学习目标 / 前置知识 / 阅读时间 / ProjectCard props”文本要直接提升为 frontmatter 字段，不要另起一套新措辞。</action>
  <verify>
    <automated>node scripts/check-learning-metadata.mjs && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "contentId: (book-00-agent-intro|book-01-agent-basics|book-03-tool-system|book-04-session-management|practice-p01-minimal-agent|practice-p10-react-loop|intermediate-27-planning-mechanism)" docs/00-what-is-ai-agent/index.md docs/01-agent-basics/index.md docs/03-tool-system/index.md docs/04-session-management/index.md docs/practice/p01-minimal-agent/index.md docs/practice/p10-react-loop/index.md docs/intermediate/27-planning-mechanism/index.md` 返回 7 条结果。
- `rg -n "entryMode: (read-first|build-first|bridge)" docs/00-what-is-ai-agent/index.md docs/practice/p01-minimal-agent/index.md docs/intermediate/27-planning-mechanism/index.md` 返回 3 条结果。
- 第 1 批完成后，`node scripts/check-learning-metadata.mjs` 至少已经能定位缺失页而不是报 schema 常量缺失；第 2、3 批继续使用同一命令作为收敛门槛。
- `node scripts/check-learning-metadata.mjs` 退出码为 0。
  </acceptance_criteria>
  <done>后续路径定义已经有 7 个稳定 content 节点可引用，不需要从正文 blockquote 或表格里反向解析。</done>
</task>

</tasks>

<verification>
先运行 `bun run typecheck`，再运行 `node scripts/check-learning-metadata.mjs`。本 plan 完成后，后续任何 learning path / section summary 实现都必须只依赖 seed frontmatter，不允许再从正文正则抽取学习目标、前置知识或预计耗时。
</verification>

<success_criteria>
`.vitepress/theme/data/content-meta.ts` 成为 Phase 1 的唯一 metadata 合同；`check-learning-metadata.mjs` 已进入 `build:strict`；7 个核心页面具备可聚合的结构化 frontmatter。
</success_criteria>

<output>
After completion, create `.planning/phases/01-learning-path-and-metadata-foundation/01-01-SUMMARY.md`
</output>
