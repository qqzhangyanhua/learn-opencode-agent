---
phase: 01-learning-path-and-metadata-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .vitepress/theme/data/content-index.data.ts
  - .vitepress/theme/data/learning-paths.data.ts
  - scripts/check-learning-paths.mjs
  - package.json
  - .vitepress/theme/components/LearningPath.vue
  - .vitepress/theme/components/SectionRoleGrid.vue
  - docs/learning-paths/index.md
autonomous: true
requirements:
  - IA-02
  - DISC-02
must_haves:
  truths:
    - "用户可以访问 `/learning-paths/`，看到按目标组织的清晰推荐顺序。"
    - "路径顺序和板块定位来自 build-time 聚合数据，而不是页面内硬编码数组。"
    - "无效路径引用、重复 pathId 或乱序步骤会在脚本阶段被拦截。"
  artifacts:
    - path: ".vitepress/theme/data/content-index.data.ts"
      provides: "标准化内容索引与 section summary"
    - path: ".vitepress/theme/data/learning-paths.data.ts"
      provides: "3 条学习路径定义、7 个实践阶段摘要与派生 path index"
    - path: "scripts/check-learning-paths.mjs"
      provides: "路径引用和顺序校验"
    - path: "docs/learning-paths/index.md"
      provides: "学习路径总览入口页"
    - path: ".vitepress/theme/components/SectionRoleGrid.vue"
      provides: "理论篇 / 实践篇 / 中级篇统一定位卡片"
  key_links:
    - from: ".vitepress/theme/data/learning-paths.data.ts"
      to: ".vitepress/theme/data/content-index.data.ts"
      via: "contentId -> content node 匹配"
      pattern: "steps:|contentId:"
    - from: "docs/learning-paths/index.md"
      to: ".vitepress/theme/components/LearningPath.vue"
      via: "组件消费 loader 输出"
      pattern: "<LearningPath"
    - from: "scripts/check-learning-paths.mjs"
      to: ".vitepress/theme/data/learning-paths.data.ts"
      via: "校验 pathId / step order / recommendedStart"
      pattern: "theory-first|practice-first|engineering-depth"
---

<objective>
把 seed metadata 聚合成可复用的数据层，并交付第一版学习路径总览页。

Purpose: 让 “按目标选路线” 从文案承诺变成真实的数据能力，同时把理论篇 / 实践篇 / 中级篇的角色定义固定为统一数据源。
Output: content index、learning path index、path validator、`/learning-paths/` 入口页和数据驱动组件。
</objective>

<execution_context>
@/Users/zhangyanhua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyanhua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-VALIDATION.md
@.planning/phases/01-learning-path-and-metadata-foundation/01-01-PLAN.md
@.vitepress/theme/components/LearningPath.vue

<interfaces>
Executor should build against these contracts from `01-01`:

```ts
export interface LearningPathStep {
  order: number
  contentId: string
  reason: string
}

export interface LearningPathDefinition {
  pathId: 'theory-first' | 'practice-first' | 'engineering-depth'
  title: string
  goal: string
  audience: string[]
  recommendedStart: string
  entryCriteria: string[]
  outcomes: string[]
  steps: LearningPathStep[]
}

export interface SectionRoleSummary {
  sectionId: 'theory' | 'practice' | 'intermediate'
  title: string
  roleDescription: string
  entryMode: 'read-first' | 'build-first' | 'bridge'
  recommendedStart: string
  countLabel: string
}

export interface PracticePhaseSummary {
  phaseId: 'phase-1' | 'phase-2' | 'phase-3' | 'phase-4' | 'phase-5' | 'phase-6' | 'phase-7'
  order: 1 | 2 | 3 | 4 | 5 | 6 | 7
  title: string
  subtitle: string
  summary: string
  projectCount: number
  projectIds: string[]
  recommendedStart: string
  themeTags: string[]
}

export const sectionIndex: SectionRoleSummary[]
export const sectionById: Record<'theory' | 'practice' | 'intermediate', SectionRoleSummary>
export const practicePhases: PracticePhaseSummary[]
```

Planned canonical path IDs and exact intent:
- `theory-first`: 先看懂 Agent 主链路
- `practice-first`: 先跑通第一个可运行 Agent
- `engineering-depth`: 从最小闭环走到工程判断
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 build-time 内容索引、路径定义和路径校验</name>
  <files>
.vitepress/theme/data/content-index.data.ts
.vitepress/theme/data/learning-paths.data.ts
scripts/check-learning-paths.mjs
package.json
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
- .planning/phases/01-learning-path-and-metadata-foundation/01-VALIDATION.md
- .planning/phases/01-learning-path-and-metadata-foundation/01-01-PLAN.md
- .vitepress/theme/data/content-meta.ts
- package.json
  </read_first>
  <action>创建 `.vitepress/theme/data/content-index.data.ts`，使用 `createContentLoader('**/index.md')` 仅聚合带 `contentId` 的页面，输出 `contentNodes`、`contentById`、`sectionIndex`、`sectionById` 四个结构。`sectionIndex` 必须是稳定顺序数组，顺序固定为 `theory -> practice -> intermediate`；`sectionById` 必须由同一份数组派生，类型固定为 `Record<'theory' | 'practice' | 'intermediate', SectionRoleSummary>`，供后续 `sectionById.practice` / `sectionById.intermediate` 这种对象式访问使用。3 个板块条目固定为：`theory`（`title: 理论篇`，`roleDescription: 看懂真实 AI Agent 系统为什么这样设计`，`entryMode: read-first`，`recommendedStart: /00-what-is-ai-agent/`，`countLabel` 表达理论章节总量）、`practice`（`title: 实践篇`，`roleDescription: 把关键机制亲手跑通并形成实现手感`，`entryMode: build-first`，`recommendedStart: /practice/p01-minimal-agent/`，`countLabel` 表达实践项目总量）、`intermediate`（`title: 中级篇`，`roleDescription: 处理稳定性、协作、安全与成本等工程专题`，`entryMode: bridge`，`recommendedStart: /intermediate/27-planning-mechanism/`，`countLabel` 表达中级专题总量）。创建 `.vitepress/theme/data/learning-paths.data.ts`，导出 3 条路径：1) `theory-first` 步骤顺序固定为 `book-00-agent-intro -> book-01-agent-basics -> book-03-tool-system -> book-04-session-management -> practice-p01-minimal-agent -> practice-p10-react-loop -> intermediate-27-planning-mechanism`；2) `practice-first` 顺序固定为 `practice-p01-minimal-agent -> book-00-agent-intro -> book-03-tool-system -> practice-p10-react-loop -> book-04-session-management -> intermediate-27-planning-mechanism`；3) `engineering-depth` 顺序固定为 `book-01-agent-basics -> book-03-tool-system -> book-04-session-management -> practice-p10-react-loop -> intermediate-27-planning-mechanism`。同文件内再导出 `practicePhases`，并按 `PracticePhaseSummary` 的精确字段落盘：`phaseId` 固定为 `phase-1` 到 `phase-7`，`order` 固定为 `1` 到 `7`，每项都要有 `title`、`subtitle`、`summary`、`projectCount`、`projectIds`、`recommendedStart`、`themeTags`；数据真源使用当前 `docs/practice/index.md` 的 7 个 phase 标题与 subtitle。新增 `scripts/check-learning-paths.mjs`，校验 `pathId` 唯一、`steps[].order` 连续递增、`steps[].contentId` 全部存在于 `contentById`、`recommendedStart` 等于首个 step 的页面 URL，且 `sectionById` 的 3 个 key 与 `sectionIndex` 数组完全一致。把 `check:learning-paths` 接入 `package.json`，并把 `build:strict` 更新为同时执行 `check:learning-metadata` 和 `check:learning-paths`。</action>
  <verify>
    <automated>node scripts/check-learning-paths.mjs && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "pathId: 'theory-first'|pathId: 'practice-first'|pathId: 'engineering-depth'" .vitepress/theme/data/learning-paths.data.ts` 返回 3 条结果。
- `rg -n "sectionById|Record<'theory' \\| 'practice' \\| 'intermediate', SectionRoleSummary>" .vitepress/theme/data/content-index.data.ts` 至少返回 2 条结果。
- `rg -n "practicePhases" .vitepress/theme/data/learning-paths.data.ts` 返回至少 1 条结果。
- `rg -n "phaseId: 'phase-1'|'phase-7'|projectIds:" .vitepress/theme/data/learning-paths.data.ts` 至少返回 3 条结果。
- `rg -n "\"check:learning-paths\": \"node scripts/check-learning-paths.mjs\"" package.json` 返回 1 条结果。
- `node scripts/check-learning-paths.mjs` 退出码为 0。
  </acceptance_criteria>
  <done>路径和板块信息已经可由 build-time 数据直接导出，且非法路径定义会被脚本拦截。</done>
</task>

<task type="auto">
  <name>Task 2: 交付学习路径总览页与可复用展示组件</name>
  <files>
.vitepress/theme/components/LearningPath.vue
.vitepress/theme/components/SectionRoleGrid.vue
docs/learning-paths/index.md
  </files>
  <read_first>
- .planning/phases/01-learning-path-and-metadata-foundation/01-RESEARCH.md
- .vitepress/theme/components/LearningPath.vue
- .vitepress/theme/data/learning-paths.data.ts
- .vitepress/theme/data/content-index.data.ts
  </read_first>
  <action>重构 `.vitepress/theme/components/LearningPath.vue`，移除当前内嵌 `const phases` 数组，改为从 `../data/learning-paths.data` 读取路径数据；组件新增可选 `pathIds?: string[]` 过滤能力，默认展示全部 3 条路径；每个路径卡片必须渲染 `title`、`goal`、`audience`、`recommendedStart`、`steps` 和 CTA。新增 `.vitepress/theme/components/SectionRoleGrid.vue`，从 `content-index.data.ts` 读取 `sectionIndex` 数组渲染 3 张卡片，但组件内部不得自己重建 keyed map；需要对象式访问时统一使用 loader 已导出的 `sectionById`。创建 `docs/learning-paths/index.md`，页面结构固定为 `# 学习路径`、`## 按目标选路径`、`## 三大内容板块怎么分工` 三段，局部导入 `LearningPath` 与 `SectionRoleGrid` 组件，不要把路径数组再写回 markdown 里。</action>
  <verify>
    <automated>bun run build:strict</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "const phases" .vitepress/theme/components/LearningPath.vue` 无结果。
- `rg -n "pathIds\\?:" .vitepress/theme/components/LearningPath.vue` 返回至少 1 条结果。
- `rg -n "^# 学习路径|## 按目标选路径|## 三大内容板块怎么分工" docs/learning-paths/index.md` 返回 3 条结果。
- `bun run build:strict` 退出码为 0。
  </acceptance_criteria>
  <done>`/learning-paths/` 已经可以显示基于统一数据源的路径卡片和板块角色说明，不再依赖手写数组。</done>
</task>

</tasks>

<verification>
必须先通过 `node scripts/check-learning-paths.mjs`，再执行 `bun run build:strict`。若 `LearningPath.vue` 仍保留内嵌阶段数组或 `docs/learning-paths/index.md` 仍写死路径步骤，本 plan 视为未完成。
</verification>

<success_criteria>
仓库里存在统一的 `content index + learning path index + section index`；`/learning-paths/` 可构建；路径定义和实践阶段摘要在一个数据层中维护。
</success_criteria>

<output>
After completion, create `.planning/phases/01-learning-path-and-metadata-foundation/01-02-SUMMARY.md`
</output>
